{
  "nixpacksPlan": {
    "phases": {
      "setup": {
        "nixPkgs": ["python311"]
      },
      "install": {
        "cmds": ["pip install -r requirements.txt"]
      },
      "build": {
        "cmds": []
      }
    },
    "start": {
      "cmd": "gunicorn app:app --bind 0.0.0.0:$PORT"
    }
  }
}
